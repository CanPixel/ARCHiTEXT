# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Optional but recommended for a nicer interactive multi-select UI.
# The CLI also has a no-dependency fallback picker, so it remains usable
# before this gem is installed.
gem "tty-prompt", "~> 0.23"
